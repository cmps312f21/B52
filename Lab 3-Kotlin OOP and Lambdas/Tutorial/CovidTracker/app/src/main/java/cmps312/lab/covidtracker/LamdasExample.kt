package cmps312.lab.covidtracker

fun main() {
    var numbers = listOf(1, 2, 3, 4, 5, 6);

//    numbers.forEach(::displayImproved1)
//    numbers.forEach { num -> println(num) }
//    numbers.forEach { println(it) }

    numbers.forEach {  }
    numbers.reduce { }
    numbers.sumBy {  }
    numbers.count()
    numbers.sortedBy {  }
    numbers.map {  }

//    for (num in numbers) display(num)
}

fun display(a: Int) {
    println(a)
}

fun displayImproved1(a: Int) = println(a)

//change this into lamdas
//(a: Int) -> println(a)


